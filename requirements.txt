# MCP Server 의존성 패키지 목록
# pip install -r requirements.txt 명령으로 한 번에 설치 가능

# MCP(Model Context Protocol) 서버 SDK
# LLM이 Tool을 사용할 수 있게 해주는 프로토콜 구현체
mcp[cli]>=1.27.0

# HTTP 클라이언트 라이브러리
# kdb_mw REST API 호출에 사용
httpx>=0.28.0

# .env 파일 읽기 라이브러리
# 환경변수(토큰, 서버 주소 등)를 .env 파일에서 로드
python-dotenv>=1.0.0
